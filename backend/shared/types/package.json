{
  "name": "@shared/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Wakala",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}